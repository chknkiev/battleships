function ship(length, hit, isSunk) {
  return {
    length,
    hit(n) {
      //If n = ship position
      //Mark as hit
    },
    isSunk() {
      //use length to determine positions hit
    }
  }
}

function gameboard() {

  function receiveAttack() {
    //Take 2 coordinates
    //Hit or miss?
    //send result
  }


  //Display all shots

  //Display winner results
}


function player() {

}